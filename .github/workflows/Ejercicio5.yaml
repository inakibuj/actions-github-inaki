name: Solo Documentacion en Develop

on:
  push:
    branches:
      - develop 
    paths:
      - 'docs/**' 

jobs:
  build-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Actualizando Documentación
        run: echo "Se han detectado cambios en la carpeta docs de la rama develop. Procesando..."